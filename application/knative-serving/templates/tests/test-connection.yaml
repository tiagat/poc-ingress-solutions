apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knative-serving.fullname" . }}-test-connection"
  labels:
    {{- include "knative-serving.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knative-serving.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
