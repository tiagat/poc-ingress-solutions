apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name:  demo-server
  namespace: default
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: default
    name: in-cluster
  source:
    path: "applications/demo-server"
    repoURL: https://github.com/tiagat/poc-ingress-solutions
    targetRevision: main
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
